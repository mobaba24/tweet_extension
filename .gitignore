# Local training/sample data — not part of the published extension
sample image
tweets.json
tweets.csv

# Python tool (xtool) — local env, outputs, caches
xtool/.venv/
xtool/out/
__pycache__/
*.pyc

# engage bots — local env, secrets, outputs
engage/.venv/
engage/.env
engage/out/

# macOS
.DS_Store
engage/stats.json
